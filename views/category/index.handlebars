<h4>categorys: </h4>
{{#each category}}
    <div class="card">
        <div class="card-body mt-2">
           <a href="/category/{{slug}}"><h4>{{name}}</h4></a> 
            </div>
    </div>
{{else}}

{{/each}}